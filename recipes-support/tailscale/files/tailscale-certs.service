[Unit]
Description=Tailscale TLS certificate generation
After=tailscaled.service
Requires=tailscaled.service

[Service]
Type=oneshot
User=tailscale
Group=tailscale
ExecStart=/usr/libexec/tailscale-certs.sh
