software =
{
		version = "@@RPI_SECURE_VERSION@@";

		files: (
			{
				name      = "pieeprom-signed";
				version   = "@@RPI_SECURE_VERSION@@";
				filename  = "pieeprom-signed.tar.gz";
				path      = "/boot";
				type	  = "archive";
				encrypted = true;
				sha256    = "$swupdate_get_sha256(pieeprom-signed.tar.gz)";
            }
        );
}
