# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a packages directory, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "raspberrypi-secure"
BBFILE_PATTERN_raspberrypi-secure := "^${LAYERDIR}/"
BBFILE_PRIORITY_raspberrypi-secure = "9"
LAYERSERIES_COMPAT_raspberrypi-secure = "scarthgap"
LAYERDEPENDS_raspberrypi-secure = "core raspberrypi integrity security harden-layer"
