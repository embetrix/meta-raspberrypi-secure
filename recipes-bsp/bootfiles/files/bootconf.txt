[all]
SIGNED_BOOT=1

# Try SD first (1), followed by USB (RP1), NVMe PCIe, then network
#BOOT_ORDER=0xf2461

# Enable UART for bootloader but can be disabled for production
BOOT_UART=1

# Disabled: bootloader watchdog not cancelled under SIGNED_BOOT=1 (firmware bug?)
# https://github.com/raspberrypi/firmware/issues/2023
# Re-enable once firmware fixes auto-cancel with secure boot
#BOOT_WATCHDOG_TIMEOUT=180

# Walk partitions if autoboot.txt is corrupt/missing
PARTITION_WALK=1

#ENABLE_SELF_UPDATE=0
